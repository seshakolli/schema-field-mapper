# Pinned to the 1.x line: `messages.parse(output_format=...)` and
# `response.parsed_output` are 1.x APIs. Verified against anthropic 1.0.0.
anthropic>=1.0.0,<2.0.0
pydantic>=2.9,<3.0
python-dotenv>=1.0,<2.0
pytest>=8.0,<9.0
